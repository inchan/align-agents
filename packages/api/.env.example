# ===========================================
# API 서버 환경 설정 템플릿
# ===========================================
# 이 파일을 복사하여 .env 또는 .env.local 파일을 생성하세요
# cp .env.example .env.local

# 환경 구분 (development | staging | production)
NODE_ENV=development

# API 서버 포트
PORT=3001

# 로그 레벨 (debug | info | warn | error)
LOG_LEVEL=info

# CORS 허용 오리진 (쉼표로 구분)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
