{
  "name": "ai-cli-syncer",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=!@ai-cli-syncer/api",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "AI CLI Syncer - Manage and Sync AI CLI Configurations",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.14",
    "turbo": "^2.6.1",
    "vitest": "^4.0.14"
  }
}